import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        justifyContent: 'center',
        alignItems: 'center',
    },
    map: {
        flex: 1,
        width: '100%',
    },
    locationTitle: {
        fontSize: 22,
    },
    locationText: {
        fontSize: 18,
    },
    locationButton: {
        margin: 10,
    },
});